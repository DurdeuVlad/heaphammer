package com.dwurdy.heaphammer.domain;

/** Deterministic lifecycle action used by the player-churn scenario. */
public enum PlayerAction {
    JOIN,
    QUIT,
    RESPAWN,
    DIMCHANGE,
    TELEPORT
}
