# NeoForge loader build — independent Gradle project nested inside the version branch.
# Builds the shared src/ tree against NeoForge via ModDevGradle.
# These pins are PER-BRANCH: the sync workflow preserves branch-local values.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# NeoForge / ModDevGradle versions for this branch's Minecraft version
neoforge_version=21.4.157
mdg_version=2.0.146
